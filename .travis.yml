git:
  depth: 10

branches:
  only:
    - master

cache:
  directories:
    - $HOME/downloads

install:
  - ./setup-travis.sh

before_script:
  - $HOME/bin/tmp/emacs/bin/emacs -q -Q -l init.el

# Build the website
script:
  - ./deploy.sh
